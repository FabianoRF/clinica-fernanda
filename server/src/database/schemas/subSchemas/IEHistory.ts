export default interface IEHistory {
  inplant: boolean;

  tratament_estetic: {
    use: boolean;
    description: string;
  };

  plastic_surgery: {
    use: boolean;
    description: string;
  };
}
